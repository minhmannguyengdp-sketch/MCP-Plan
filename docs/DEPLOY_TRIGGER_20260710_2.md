# Deploy trigger

Trigger Vercel production deployment after MCP audit fixes:

- session report aggregation from all session-linked rows
- follow-up details in BC phiên
- real /api/accounts/data for customers
- customer revenue scoped to current month

Created to re-run Vercel after prior auto deployments were canceled.
